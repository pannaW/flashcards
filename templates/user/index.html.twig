{% extends 'layout.html.twig' %}

    {% block title %}
        All users
    {% endblock %}


    {% block body %}
        <h1> All users </h1>

        {% if paginator.data|length %}
            {% for row in paginator.data %}
                <li><a href="{{ url('user_view', {'id': row.id }) }}">
                        {{ row.id }}, {{ row.login }}
                    </a></li>
            {% endfor %}
            {% include 'default/_paginator.html.twig' with {
            paginator: paginator,
            route_name: 'user_index_paginated',
            } %}
        {% else %}
            <div>
                No users found
            </div>
        {% endif %}

    {% endblock %}

