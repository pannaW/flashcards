{% extends 'panel.html.twig' %}

    {% block title %}
        Your tags
    {% endblock %}


    {% block board %}

        {% block header %}
            <h1> Your tags </h1>
        {% endblock %}

        {% block article %}
            {% if tag|length %}
                {% for row in tag %}
                    <li>
                            {{ row.id }}, {{ row.name }}
                        <a href="{{ url('tag_delete', {'id': row.id }) }}"><i class="fa fa-remove"></i></a>
                    </li>
                {% endfor %}

            {% else %}
                <div>
                    No tags found
                </div>
            {% endif %}
        {% endblock %}

    {% endblock %}

